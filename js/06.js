// Math

let resultado 

resultado = Math.PI 
resultado = Math.round(2.4)
resultado = Math.ceil(2.1) // Redondea hacia arriba
resultado = Math.floor(2.6) // Redondea hacia abajo
resultado = Math.sqrt(2) // Raiz cuadrada
resultado = Math.abs(200) // Canbia a positivo
resultado = Math.min(3, 4, 5, 9, 1, 10) // Minimo de un listado
resultado = Math.max(3, 4, 5, 9, 1, 10) // Maximo de un listado
resultado = Math.random();
resultado = Math.floor( Math.random() * 30); // Random hasta el 30

console.log(resultado)